apiVersion: v1
kind: Service
metadata:
  name: auth-service
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8001
  selector:
    app.kubernetes.io/name: {{ include "auth-service.name" . }}
