apiVersion: v1
kind: Service
metadata:
  name: core-service
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8002
  selector:
    app.kubernetes.io/name: {{ include "core-service.name" . }}
