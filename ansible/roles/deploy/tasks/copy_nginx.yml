- name: Ensure api-gateway directory exists
  file:
    path: "{{ deploy_path }}/api-gateway"
    state: directory
    mode: "0755"

- name: Copy nginx.conf
  template:
    src: nginx.conf.j2
    dest: "{{ deploy_path }}/api-gateway/nginx.conf"
    mode: "0644"
  notify:
    - reload nginx
